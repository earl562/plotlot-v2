# assets/__init__.py — makes the assets/ directory an importable Python sub-package
# Required so that "from .assets.ingestion import ..." works in dagster_project/__init__.py
# Empty file is sufficient — just needs to exist for Python to treat this as a package
