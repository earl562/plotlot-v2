# Packages

This directory is reserved for shared libraries used by one or more apps in `apps/`.

Add new shared code as subfolders under `packages/` (for example `packages/shared-python/` or `packages/shared-ui/`).
